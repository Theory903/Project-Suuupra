apiVersion: v1
kind: Service
metadata:
  name: identity
  namespace: suuupra
  labels:
    app: identity
spec:
  type: ClusterIP
  selector:
    app: identity
  ports:
    - name: http
      port: 8081
      targetPort: 8081


